#!/bin/bash -e

function has_session() {
    set +e
    tmux list-sessions &>/dev/null; rc=$?
    set -e
    return $rc
}


if has_session
then
    urxvt -e tmux a "$@"
else
    urxvt -e tmux new-session "$@"
fi
